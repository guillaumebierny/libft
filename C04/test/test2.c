#include <stdio.h>

int main()
{
	char tab = '\t';
	char line = '\n';
	char v = '\v';
	char f ='\f';
	char r = '\r';
	char espace = ' ';

	printf("%d, %d, %d, %d, %d, %d", tab, line, v, f, r, espace);
}
